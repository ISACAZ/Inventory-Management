# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Data validation
pydantic==2.7.0
pydantic-settings==2.2.1     # for config/.env loading

# Database
sqlalchemy==2.0.30
pymysql==1.1.0           # or pymysql==1.1.0 (ask DB teammate)
alembic==1.13.1               # DB migrations (even if DB guy owns schema)

# Auth
python-jose[cryptography]==3.3.0   # JWT
passlib[bcrypt]==1.7.4             # password hashing

# Dev / testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0                 # for TestClient
python-dotenv==1.0.1

# Utilities
python-multipart==0.0.9       # for form data / file uploads later
